flask>=3.0.0
gunicorn>=22.0.0
yfinance>=0.2.28
tkcalendar
openpyxl

# ── Security floors on transitive dependencies ───────────────────────────────
# Not imported here — pinned only to keep a resolver from installing a version
# with a known high-severity advisory.  Drop a line once its direct parent
# requires the fixed version on its own.
soupsieve>=2.8.4   # via beautifulsoup4 (yfinance): CVE-2026-49476/49477, 7.5 HIGH — ReDoS + memory exhaustion in the selector parser
idna>=3.15         # via requests: CVE-2026-45409, 5.3 MEDIUM
